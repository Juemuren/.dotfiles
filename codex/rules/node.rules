prefix_rule(
    pattern = [["npm", "pnpm"], "run"],
    decision = "prompt",
    justification = "package scripts execute repository-defined code; approve out-of-sandbox use case by case",
    match = ["npm run build", "pnpm run test"],
)
