# AGENTS.md

## Shell

- 使用 `sh path/to/script.sh` 运行 Bash 脚本
- 使用 `pwsh -NoProfile path/to/script.ps1` 运行 PowerShell 脚本

## 文件查找与代码搜索

- 按文件或路径名查找文件条目时优先使用 `fd`
- 搜索文件内容时优先使用 `rg`
- 需要根据代码结构进行复杂搜索时优先使用 `ast-grep`
